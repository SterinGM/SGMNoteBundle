<?xml version="1.0" encoding="UTF-8" ?>

<routes xmlns="http://symfony.com/schema/routing"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/routing http://symfony.com/schema/routing/routing-1.0.xsd">

    <route id="sgm_note_list" pattern="/">
        <default key="_controller">SGMNoteBundle:Note:list</default>
    </route>

    <route id="sgm_note_new" pattern="/new">
        <default key="_controller">SGMNoteBundle:Note:new</default>
    </route>

    <route id="sgm_note_edit" pattern="/{noteId}">
        <default key="_controller">SGMNoteBundle:Note:edit</default>
    </route>

    <route id="sgm_note_delete" pattern="/{noteId}/{token}">
        <default key="_controller">SGMNoteBundle:Note:delete</default>
    </route>

</routes>
